# ─── RAG-пайплайн (core) ──────────────────────────────────────────────
chromadb==0.5.23
sentence-transformers==3.3.1
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
python-dotenv==1.0.1

# ─── Edge TTS (Piper ONNX, для edge_tts_server.py на G1) ─────────────
piper-tts==1.2.0

# ─── Robot controller (для robot_controller.py на G1) ─────────────────
# pyaudio больше НЕ нужен — воспроизведение через unitree_sdk2 AudioClient
# (httpx, fastapi, uvicorn уже в core выше)

# ─── Unitree SDK2 (ставится ОТДЕЛЬНО на G1, не через pip) ────────────
# git clone https://github.com/unitreerobotics/unitree_sdk2_python.git
# cd unitree_sdk2_python && pip install -e .
# См. EDGE_README.md

# ─── TTS-сервер (GPT-SoVITS) и подготовка аудио ставятся ОТДЕЛЬНО ─────
# См. TTS_README.md. Они конфликтуют с RAG-зависимостями, поэтому:
#
# GPT-SoVITS (для tts_server.py):
#   git clone https://github.com/RVC-Boss/GPT-SoVITS
#   cd GPT-SoVITS && pip install -r requirements.txt
#
# Подготовка аудио (для audio_prep.py):
#   pip install openai-whisper torch torchaudio demucs
#   + ffmpeg в системе
